# UI / 컨셉 목업 — 『잊혀진 발자국 (Lost Pawprint)』

상위 기획: [../GDD.md](../GDD.md) · 결정 로그: [../00_design_review.md](../00_design_review.md) · 미스터리·서스펜스 톤
팔레트: **검정(#06090d) + 시안 글로우(#34e2e2) + 형광 연두(#8ef548)** — 주인공 캐릭터 색에서 확장.
> **v2 (D9 반영):** 디자인 시스템·메모리 미터 4상태·기억 일지·설정/접근성 화면 추가. 색 역할 고정(시안=중립UI / 연두=긍정·해금) + 색+형태 이중 인코딩 + 광과민성 가드.

## 화면 목록

| 파일 | 화면 | 핵심 요소 |
|---|---|---|
| `01_main_menu.svg` | 메인 메뉴 | 캐릭터, 타이틀, 떠다니는 발바닥 모티프, "새 기억(New Game)" 등 메뉴 |
| `02_gameplay_hud.svg` | 게임플레이 HUD | 탑다운 방, **메모리 미터(발바닥 10칸)**, 정체성 초상(?), 기억 조각([E] 조사), 적(Murk)+시야 콘, 안전지대, 살금살금 버튼 |
| `03_identity_card.svg` | 정체성 카드 | 초상 진화(? → 실루엣 → 눈 → 얼굴 → 이름), 5개 슬롯(종/집/주인/이름/왜 잊었나) |
| `04_memory_cutscene.svg` | 기억 조각 회상 컷신 | 따뜻한 색 회상 프레임, 자막, "정체성 해금 + 기억 +1칸" 토스트 |
| `05_low_memory_suspense.svg` | 기억 위험(서스펜스) | 채도 저하·비네팅·색수차, 다가오는 적들, 환각(가짜 조각), 메모리 1/10(적색) |
| `06_design_system.svg` | **디자인 시스템** (v2) | 색 토큰(역할 고정), **메모리 미터 4상태**(empty/half/full/danger·0.5칸), 색+형태 이중 인코딩, **시점 규칙**(정면치비=초상 / 3·4쿼터뷰=인게임+발자국 트레일) |
| `07_journal_clueboard.svg` | **기억 일지·추리 보드** (v2) | 타임라인(수집/빠진/거짓 조각), 관계도(단서 연결·미지 노드), 새 추론 잠금 해제, 중심 미스터리 진행 |
| `08_settings_accessibility.svg` | **설정·접근성** (v2) | 색약 3모드, 텍스트 크기, **화면 흔들림/플래시/색수차 끄기(광과민성)**, 적 속도, 퍼즐 스킵, 음성 낭독, 난이도, 사별 콘텐츠 안내 |
| `png/board.png` | 컨셉 보드 v2 | 위 8개를 한 장에 모은 요약 시트 |

> 렌더 결과 PNG는 `png/` 폴더에 있습니다. (`png/board.png`가 전체 요약)

## 다시 렌더링하기

SVG가 원본(소스)이며, 아래로 PNG와 보드를 재생성합니다.

```bash
pip install cairosvg pillow
# 한글 폰트 필요: Noto Sans CJK KR  (render.py의 FONT 경로 참고)
python3 render.py     # 8개 화면 PNG + png/board.png 생성
```

개별 SVG는 어떤 브라우저/벡터 툴에서도 바로 열람·편집 가능합니다.
색·문구·레이아웃을 SVG에서 수정한 뒤 `render.py`로 다시 뽑으면 됩니다.

## Figma로 가져가려면
각 SVG를 Figma 캔버스에 드래그&드롭하면 벡터 레이어로 임포트됩니다.
이후 컴포넌트화(메모리 미터 1칸, 슬롯, 토스트 등)하여 디자인 시스템으로 확장하는 것을 권장합니다.
